# T002 - Strict configuration

**Status:** complete

Implement typed YAML loading with unknown keys forbidden, path normalization, runtime-directory
creation, token validation for bot/worker modes, config-check command, and tests.
