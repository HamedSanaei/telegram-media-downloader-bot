# Cookies

Place an operator-managed Netscape-format cookie file here as `cookies.txt` when a supported site
requires authenticated access. This directory is ignored except for this README.

Never commit real cookies. Prefer a dedicated low-privilege account. Rotate cookies if they are
exposed. The application reads the path configured in `config.yaml`.
